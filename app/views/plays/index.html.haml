%h1.current-category= params[:category]

- if @plays.count ==0
  %h3 There are no Plays in this Category
- else
  .row
    - @plays.each do |play|
      .col-md-3
        %a{:href => "/plays/#{play.id}"}
          = image_tag play.play_img.variant(resize: "250x350>")
